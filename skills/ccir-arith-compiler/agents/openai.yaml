interface:
  display_name: "CCIR Arithmetic Compiler"
  short_description: "Compile quantitative clauses into validated ASL-Arith"
  default_prompt: "Compile the supplied teacher request into the required ASL response JSON."
